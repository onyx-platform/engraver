---
# File automatically generated by Engraver.
# DO NOT MODIFY.

% for p in profiles:
- hosts: localhost
  remote_user: "{{ remote_user }}"
  become_method: sudo
  vars_files:
    - "group_vars/{{ onyx_cluster_id }}.yml"
    - "vars/cluster_vars/{{ onyx_cluster_id }}/machine_profiles/${ p }_profile.yml"
  vars:
    - populate_inventory: yes
  roles:
    - aws
% endfor

% for p in profiles:
- hosts: ${ p }
  remote_user: "{{ remote_user }}"
  become_method: sudo
  vars_files:
    - "roles/zookeeper/defaults/main.yml"
    - "group_vars/{{ onyx_cluster_id }}.yml"
    - "vars/cluster_vars/{{ onyx_cluster_id }}/machine_profiles/${ p }_profile.yml"
  vars:
    - kill: yes
  roles:
    - onyx

% endfor
