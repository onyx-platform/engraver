---
# File automatically generated by Engraver.
# DO NOT MODIFY.

- hosts: localhost
  remote_user: "{{ remote_user }}"
  become_method: sudo
  vars_files:
    - "group_vars/{{ onyx_cluster_id }}.yml"
  vars:
    - create_vpc: no
    - create_ec2: no
    - cache: yes
  roles:
    - aws
