---
# Autogenerated file by Engraver. You may fill this playbook
# out with any tasks that need to run after Engraver has provisioned
# your cluster.

- hosts: localhost
  remote_user: "{{ remote_user }}"
  become_method: sudo
