---
# File automatically generated by Engraver.
# DO NOT MODIFY.

- hosts: localhost
  remote_user: "{{ remote_user }}"
  vars:
    - create_vpc: yes
  roles:
    - aws

% for p in profiles:
- hosts: localhost
  remote_user: "{{ remote_user }}"
  vars_files:
    - vars/machine_profiles/${ p }
  vars:
    - create_vpc: no
  roles:
    - aws

% endfor
% for p in profiles:
- hosts: default
  remote_user: "{{ remote_user }}"
  vars_files:
    - vars/machine_profiles/${ p }
  roles: "{{ machine_services }}"

% endfor