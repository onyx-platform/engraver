---
# File automatically generated by Engraver.
# DO NOT MODIFY.

- hosts: localhost
  remote_user: "{{ remote_user }}"
  vars:
    - create_vpc: yes
  roles:
    - aws

- hosts: localhost
  remote_user: "{{ remote_user }}"
  vars_files:
    - vars/machine_profiles/default_profile.yml
  vars:
    - create_vpc: no
  roles:
    - aws

- hosts: default
  remote_user: "{{ remote_user }}"
  vars_files:
    - vars/machine_profiles/default_profile.yml
  roles: "{{ machine_services }}"
